import type { RequestHandler } from "express";
export declare const badgeController: {
    listBadges: RequestHandler;
    createBadge: RequestHandler;
    updateBadge: RequestHandler;
    deleteBadge: RequestHandler;
};
//# sourceMappingURL=badge.controller.d.ts.map